//C problem to print multiple word string.
#include<stdio.h>
#include<string.h>
int main(){
  char str[100];
  // Usual method for multiple word string.
 printf("Enter a string: ");
  if(scanf("%[^\n]", str));
  gets(str);
  printf("%s", str);
  return 0;

}